name: xeus-octave
channels:
  - conda-forge
dependencies:
  - xeus-octave >=0.0.7
  # For Plotly
  - plotly
  - ipywidgets
  - jupyter-dash
  # Runtime dependency for Octave ``symbolic`` package.
  - sympy
  # For compiling the Octave ``control`` and ``signal`` packages
  - compilers
  - make
